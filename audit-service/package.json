{
  "name": "audit-service",
  "version": "1.0.0",
  "description": "Audit Service with SQS, Lambda and MongoDB",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --outfile=dist/handler.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongodb": "^6.3.0",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/node": "^18.19.0",
    "@types/uuid": "^10.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}